---
name: 至禾设计 KISSH Design
description: 至禾装饰一体化工作室官网——一人统筹全流程，展示全案设计与施工落地能力。
colors:
  accent: "#30B0D0"
  accent-soft: "rgba(48, 176, 208, 0.6)"
  accent-subtle: "rgba(48, 176, 208, 0.4)"
  bg-primary: "#050A0F"
  bg-secondary: "#0A141F"
  bg-glass: "rgba(5, 10, 15, 0.25)"
  text-primary: "#EDE8E4"
  text-secondary: "rgba(237, 232, 228, 0.75)"
  text-tertiary: "rgba(237, 232, 228, 0.65)"
  text-muted: "rgba(237, 232, 228, 0.5)"
  text-faint: "rgba(237, 232, 228, 0.35)"
  white: "#FFFFFF"
  white-88: "rgba(255, 255, 255, 0.88)"
  white-85: "rgba(255, 255, 255, 0.85)"
  white-82: "rgba(255, 255, 255, 0.82)"
  white-80: "rgba(255, 255, 255, 0.80)"
  white-75: "rgba(255, 255, 255, 0.75)"
  white-65: "rgba(255, 255, 255, 0.65)"
  white-60: "rgba(255, 255, 255, 0.60)"
  white-55: "rgba(255, 255, 255, 0.55)"
  white-50: "rgba(255, 255, 255, 0.50)"
  white-40: "rgba(255, 255, 255, 0.40)"
  white-18: "rgba(255, 255, 255, 0.18)"
  white-08: "rgba(255, 255, 255, 0.08)"
  white-06: "rgba(255, 255, 255, 0.06)"
  white-02: "rgba(255, 255, 255, 0.02)"
  border: "rgba(255, 255, 255, 0.08)"
  border-light: "rgba(255, 255, 255, 0.18)"
  border-strong: "rgba(255, 255, 255, 0.5)"
  shadow: "rgba(0, 0, 0, 0.45)"
  pure-white: "#FFFFFF"
  pure-black: "#000000"
typography:
  display:
    fontFamily: "'Noto Serif SC', Georgia, serif"
    fontSize: "clamp(48px, 7vw, 96px)"
    fontWeight: 300
    lineHeight: 1
    letterSpacing: "0.12em"
  display-sm:
    fontFamily: "'Noto Serif SC', Georgia, serif"
    fontSize: "clamp(36px, 6vw, 64px)"
    fontWeight: 300
    lineHeight: 1.1
    letterSpacing: "0.1em"
  headline:
    fontFamily: "'Noto Serif SC', Georgia, serif"
    fontSize: "clamp(32px, 3vw, 52px)"
    fontWeight: 300
    lineHeight: 1.3
    letterSpacing: "normal"
  headline-sm:
    fontFamily: "'Noto Serif SC', Georgia, serif"
    fontSize: "clamp(24px, 8vw, 36px)"
    fontWeight: 300
    lineHeight: 1.35
    letterSpacing: "normal"
  title:
    fontFamily: "'Noto Serif SC', Georgia, serif"
    fontSize: "clamp(28px, 2.5vw, 42px)"
    fontWeight: 300
    lineHeight: 1.35
    letterSpacing: "normal"
  title-sm:
    fontFamily: "'Noto Serif SC', Georgia, serif"
    fontSize: "clamp(24px, 6vw, 32px)"
    fontWeight: 300
    lineHeight: 1.4
    letterSpacing: "normal"
  subhead:
    fontFamily: "'Noto Serif SC', Georgia, serif"
    fontSize: "22px"
    fontWeight: 300
    lineHeight: 1.4
    letterSpacing: "normal"
  subhead-sm:
    fontFamily: "'Noto Serif SC', Georgia, serif"
    fontSize: "18px"
    fontWeight: 300
    lineHeight: 1.4
    letterSpacing: "normal"
  lead:
    fontFamily: "'Noto Sans SC', 'Helvetica Neue', Arial, sans-serif"
    fontSize: "20px"
    fontWeight: 300
    lineHeight: 1.6
    letterSpacing: "0.08em"
  body:
    fontFamily: "'Noto Sans SC', 'Helvetica Neue', Arial, sans-serif"
    fontSize: "15px"
    fontWeight: 300
    lineHeight: 1.8
    letterSpacing: "0.02em"
  body-sm:
    fontFamily: "'Noto Sans SC', 'Helvetica Neue', Arial, sans-serif"
    fontSize: "14px"
    fontWeight: 300
    lineHeight: 1.9
    letterSpacing: "0.02em"
  body-xs:
    fontFamily: "'Noto Sans SC', 'Helvetica Neue', Arial, sans-serif"
    fontSize: "13px"
    fontWeight: 300
    lineHeight: 1.6
    letterSpacing: "0.02em"
  label:
    fontFamily: "'Noto Sans SC', 'Helvetica Neue', Arial, sans-serif"
    fontSize: "12px"
    fontWeight: 400
    lineHeight: 1
    letterSpacing: "0.25em"
  label-sm:
    fontFamily: "'Noto Sans SC', 'Helvetica Neue', Arial, sans-serif"
    fontSize: "11px"
    fontWeight: 400
    lineHeight: 1
    letterSpacing: "0.25em"
  label-xs:
    fontFamily: "'Noto Sans SC', 'Helvetica Neue', Arial, sans-serif"
    fontSize: "10px"
    fontWeight: 400
    lineHeight: 1
    letterSpacing: "0.15em"
rounded:
  none: "0"
  sm: "4px"
  md: "0.625rem"
  lg: "12px"
  pill: "40px"
spacing:
  xs: "8px"
  sm: "16px"
  md: "28px"
  lg: "48px"
  xl: "120px"
  section-y: "16vh"
  content-x: "6vw"
  gap-lg: "6vw"
components:
  button-primary:
    backgroundColor: "{colors.white-08}"
    textColor: "{colors.white}"
    rounded: "{rounded.pill}"
    padding: "14px 32px"
  button-primary-hover:
    backgroundColor: "{colors.white-18}"
    textColor: "{colors.white}"
    rounded: "{rounded.pill}"
    padding: "14px 32px"
  nav-link:
    textColor: "{colors.white-85}"
    typography: "{typography.label}"
  nav-link-hover:
    textColor: "{colors.white}"
    typography: "{typography.label}"
  skip-link:
    backgroundColor: "{colors.pure-white}"
    textColor: "{colors.bg-primary}"
    padding: "8px 16px"
---

# Design System: 至禾设计 KISSH Design

## 1. Overview: The Reliable Blueprint

**Creative North Star: "The Reliable Blueprint"**

至禾装饰的网站不是一份美学宣言，而是一份可信赖的施工蓝图。每一项服务都有明确的负责人、可执行的节点和真实的落地案例。设计拒绝传统装修公司复杂的层级转包感、过度包装的设计概念图，以及模板化的"关于我们-服务-团队-联系"结构；它追求"精简高效、扎实落地"的沟通效率。

界面密度适中，信息结构化清晰，让访客在 30 秒内理解"一人统筹全流程"的服务模式。深色背景传递沉稳可信的气质，文字像现场标注一样精确；蓝青色 accent 仅用于极少数需要引导行动的节点，强调其功能性而非装饰性。

**Key Characteristics:**

- 深色沉稳背景，区块式信息架构
- 服务流程时间轴与五大类目卡片，信息结构化
- 真实案例与具体服务条目优先于概念描述
- 文字以衬线标题建立信赖感，无衬线正文保证可读
- 动效克制、快速，服务于信息展开而非氛围营造
- 组件边界清晰但不厚重，hover 时提供明确反馈

## 2. Colors: The Midnight Ink Palette

调色板以极深蓝黑为底，暖调米白为墨，蓝青色为唯一 accent。所有颜色都带有灰度，拒绝高饱和与明亮纯白。

### Primary

- **Cyan Breath** (`#30B0D0`): 唯一的强调色，用于极少数需要引导视线的地方，如材质词汇表的英文标签、交互焦点。使用原则：稀有即价值。

### Neutral

- **Midnight Ink** (`#050A0F`): 主背景，页面绝大多数表面的底色，接近纯黑但带有一丝蓝调。
- **Deep Navy** (`#0A141F`): 次级背景，用于需要与主背景分离的层，但仍保持深色连续性。
- **Warmed Rice Paper** (`#EDE8E4`): 主文字色，暖调米白，避免冷白带来的刺眼感。
- **Dimmed Rice Paper** (`rgba(237, 232, 228, 0.75)`): 次级文字、描述性文案，降低层级但不失可读。
- **Soft Rice Paper** (`rgba(237, 232, 228, 0.65)`): 第三层级文字，如服务条目、时间轴条目。
- **Faded Ink** (`rgba(237, 232, 228, 0.5)`): 弱化文字，如页脚品牌名、辅助信息。
- **Ghost Ink** (`rgba(237, 232, 228, 0.35)`): 极淡文字，仅用于装饰性标签、分区小字。
- **Pure White** (`#FFFFFF`): 极少使用，仅用于首屏大标题、logo 等需要绝对聚焦的元素；实际代码中通过 `--color-white-*` 透明度系列控制悬浮、聚焦与玻璃层级。

### Borders & Dividers

- **Whisper Border** (`rgba(255, 255, 255, 0.08)`): 默认分隔线、组件边框。
- **Soft Border** (`rgba(255, 255, 255, 0.18)`): 悬停或聚焦时提升的边框。
- **Glass Border** (`rgba(255, 255, 255, 0.5)`): CTA 按钮、透明容器的默认边框。

### Named Rules

**The One Breath Rule.** 蓝青色 accent 在任一屏幕上只出现在 ≤10% 的功能性引导元素中。它的稀有性就是它的力量。

**The No Pure White Rule.** 除了首屏大标题与 logo，正文与说明文字统一使用暖调米白 `#EDE8E4`，拒绝冷白带来的科技感。

**The Real Over Render Rule.** 案例展示优先使用实景照片，服务描述使用具体条目而非抽象概念。

## 3. Typography: The Silent Conversation

标题使用 Noto Serif SC（思源宋体）传递东方书卷气与沉静情绪；正文使用 Noto Sans SC（思源黑体）保证长文案的可读性。整体字重偏轻，排版以留白和 letter-spacing 营造呼吸感。

**Display Font:** Noto Serif SC, Georgia, serif
**Body Font:** Noto Sans SC, "Helvetica Neue", Arial, sans-serif

### Hierarchy

- **Display** (weight 300, `clamp(48px, 7vw, 96px)`, line-height 1, letter-spacing 0.12em): 首屏品牌字 / logo 替代文字。仅用于最大视觉冲击的单一时刻。
- **Headline** (weight 300, `clamp(32px, 3vw, 52px)`, line-height 1.3): 各区块主标题，如 Hero 副标题、精选案例标题。
- **Title** (weight 300, `clamp(28px, 2.5vw, 42px)`, line-height 1.35): 子区块标题，如服务流程节点、服务类目标题。
- **Subhead** (weight 300, 22px / 18px, line-height 1.4): 时间轴阶段标题、悬浮描述等需要略大于正文但小于 Title 的文本。
- **Lead** (weight 300, 20px, line-height 1.6, letter-spacing 0.08em): 画廊项目标题、强调性导语。
- **Body** (weight 300, 15px, line-height 1.8, letter-spacing 0.02em): 描述性正文、项目详情段落。最大行宽控制在 65–75ch。
- **Body Small** (weight 300, 14px / 13px, line-height 1.6–1.9): 辅助说明、列表条目。
- **Label** (weight 400, 12px / 11px / 10px, letter-spacing 0.15em–0.3em, uppercase): 区块标签、导航链接、元数据标签。英文标签可保持全大写，中文标签保持正常大小写。10px 与 11px 仅用于非关键元数据，并保证不透明度 ≥0.55 以满足对比度。

### Named Rules

**The One Serif Voice Rule.** 衬线字体只用于 Display / Headline / Title，不用于正文或按钮，避免华丽感泛滥。

**The Wide Breath Rule.** Label 与小字使用大 letter-spacing（0.15em–0.3em）制造精致的间隙感，但正文保持正常字距以保证阅读舒适。

**The Scan-Friendly Rule.** 服务类目与时间轴条目使用列表形式呈现，便于用户快速扫描关键信息。

## 4. Elevation: Flat & Atmospheric

本系统**不使用传统投影**来营造层级。深度通过三种方式表达：

1. **色调分层**：`#050A0F` 与 `#0A141F` 的微妙色差区分不同信息区块。
2. **透明度变化**：文字、边框、按钮背景通过不透明度建立信息层级。
3. **光影质感**：文字阴影 `0 2px 24px rgba(0,0,0,0.45)` 与 backdrop-filter blur 在首屏与 CTA 上制造空气感。

### Shadow Vocabulary

- **Text Shadow** (`0 2px 24px var(--color-shadow)`): 首屏与导航文字，用于在复杂背景（WebGL 流体、图片）上保持可读性。
- **Backdrop Blur** (`blur(6px)`): CTA 按钮背景，制造磨砂玻璃般的轻微空气层。

### Named Rules

**The No Drop Shadow Rule.** 卡片、容器、按钮在默认状态下不使用 box-shadow。阴影只作为对状态的响应（hover、focus），而不是结构工具。

**The Flat Hierarchy Rule.** 服务卡片通过 1px 边框与背景色区分，不使用浮起阴影暗示层级。

## 5. Components: Refined and Restrained

所有组件都遵循"清晰可信赖"的原则：边界明确但不厚重、状态反馈即时、信息层级一目了然。

### Buttons

- **Shape:** Pill 圆角（`border-radius: 40px`）
- **Primary:** 透明背景 `rgba(255,255,255,0.08)` + 玻璃边框 `rgba(255,255,255,0.5)`，内边距 `14px 32px`，文字白色，label 样式 uppercase
- **Hover / Focus:** 背景提升至 `rgba(255,255,255,0.18)`，边框提升至 `rgba(255,255,255,0.8)`，focus 显示 `1px solid rgba(255,255,255,0.4)` 轮廓并外扩 4px
- **Transition:** `background 0.3s ease, border-color 0.3s ease`

### Navigation

- **Style:** 固定顶部，透明背景，无容器边框
- **Logo:** 高度 44px，保持原比例，最大宽度 220px
- **Links:** Label 样式，13px，letter-spacing 0.15em，默认 `rgba(255,255,255,0.85)`，hover 为 `#FFFFFF`
- **Mobile:** 当前断点为 768px，移动端布局由各区块自行处理

### Skip Link

- **Style:** 固定于视口顶部，默认隐藏在 `-40px` 之外
- **Focus:** 下滑显示，纯白背景 `#FFFFFF` + 深色文字 `#050A0F`，内边距 `8px 16px`

### Cards / Containers

- 本项目**不使用传统卡片容器**。内容直接铺在深色背景上，或通过大间距与边框线分隔。
- 若未来需要容器，使用 `rgba(255,255,255,0.08)` 边框 + `backdrop-filter: blur(6px)`，圆角取 `{rounded.md}`。

### Signature Component: WebGL Fluid Background

- **Role:** Hero、About、Gallery 三个区块的背景层，通过 Three.js 自定义 Shader 实现缓慢流动的深色流体。
- **Behavior:** 响应鼠标位置，但运动极其缓慢；通过 `IntersectionObserver` 在不可见时暂停渲染以节省 GPU。
- **Accessibility:** `prefers-reduced-motion: reduce` 时降低/禁用动态效果。

### Service Timeline

- **Role:** "关于我们"板块的核心组件，将全链条服务拆解为 4 个阶段节点。
- **Structure：**左侧数字标记 + 垂直连线，右侧阶段标题 + 具体条目列表。
- **Responsive：**移动端垂直堆叠，条目单列排列。

### Service Category Grid

- **Role：**"服务体系"板块展示五大经营类目。
- **Structure：**卡片网格，每张卡片包含类目标题与具体服务条目列表。
- **Hover：**边框颜色过渡为半透明蓝青色，背景微微提亮。

## 6. Do's and Don'ts

### Do:

- **Do** 使用 `#050A0F` 作为主背景，`#EDE8E4` 作为主文字色。
- **Do** 让蓝青色 `#30B0D0` 保持稀有，只用于最关键的引导元素。
- **Do** 使用大写、宽字距的 Label 样式处理英文分区标签。
- **Do** 保持 44px 的最小点击高度，确保导航与 CTA 的可访问性。
- **Do** 使用 `clamp()` 实现响应式字号，让标题在大屏上呼吸、在小屏上可读。
- **Do** 为所有动画提供 `prefers-reduced-motion: reduce` 的静态替代。
- **Do** 使用描述性 `alt` 文本，并确保聚焦状态清晰可见。

### Don't:

- **Don't** 使用典型的 SaaS 奶油色/蓝色模板感（柔和渐变、圆角大按钮、emoji、插画）。
- **Don't** 使用过度商业化的地产网站风格（大红大绿、密集信息、促销感）。
- **Don't** 使用过度动感的作品集效果（满屏飞入动画、视差滚动滥用、闪烁特效）。
- **Don't** 使用模板化的"关于我们-服务-团队-联系"结构。
- **Don't** 使用过于明亮的纯白背景配高饱和色彩。
- **Don't** 只展示效果图而无实景落地佐证。
- **Don't** 使用抽象概念描述服务而不列出具体服务条目。
- **Don't** 在卡片或容器上使用默认投影；深度应通过色调与透明度表达。
- **Don't** 将衬线字体用于正文或按钮。
- **Don't** 让 CTA 按钮使用高饱和背景色；透明玻璃感才是本系统的语言。
